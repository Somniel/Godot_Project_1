name: Run Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  GODOT_VERSION: "4.5.1"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: ${{ env.GODOT_VERSION }}
          use-dotnet: false

      - name: Verify Godot installation
        run: godot --version

      - name: Import project
        run: xvfb-run godot --headless --import
        timeout-minutes: 5

      - name: Run unit tests
        run: |
          xvfb-run godot --headless -s addons/gut/gut_cmdln.gd \
            -gdir=res://tests/unit \
            -ginclude_subdirs \
            -gexit \
            -glog=1
        timeout-minutes: 5

      - name: Run integration tests
        run: |
          xvfb-run godot --headless -s addons/gut/gut_cmdln.gd \
            -gdir=res://tests/integration \
            -ginclude_subdirs \
            -gexit \
            -glog=1
        timeout-minutes: 10
        continue-on-error: true  # Integration tests may fail without Steam

  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install gdtoolkit
        run: pip install gdtoolkit

      - name: Run gdlint
        run: gdlint .
        continue-on-error: true  # Warning only for now

      - name: Check formatting
        run: gdformat --check .
        continue-on-error: true  # Warning only for now
